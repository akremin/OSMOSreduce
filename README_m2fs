
In your home iraf directory, add the following two lines at the end of the login.cl file but before the final 'keep' command.

# Load m2fs package.
task $m2fs = home$m2fs.cl

[Be sure the home parameter is set to your default home iraf directory as it almost must bve for IRAF to work in the first place.]



